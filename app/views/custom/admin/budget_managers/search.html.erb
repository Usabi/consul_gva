<h2><%= t("admin.budget_managers.search.title") %></h2>

<%= render "admin/shared/user_search", url: search_admin_budget_managers_path %>

<div id="budget_managers">
  <% if @users.any? %>
    <h3><%= page_entries_info @users %></h3>

    <table>
      <thead>
        <th scope="col"><%= BudgetManager.human_attribute_name(:name) %></th>
        <th scope="col"><%= BudgetManager.human_attribute_name(:email) %></th>
        <th scope="col" class="small-3"><%= t("admin.shared.actions") %></th>
      </thead>
      <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td>
            <%= render Admin::Roles::TableActionsComponent.new(user.budget_manager||user.build_budget_manager) %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  <% else %>
    <div class="callout alert margin">
      <%= t("admin.shared.no_search_results") %>
    </div>
  <% end %>
</div>
