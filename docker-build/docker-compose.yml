services:
  build:
    build:
      context: .
      args:
        RUBY_VERSION: 3.3.5
        HOST_UID: ${HOST_UID:-1000}
    image: mi-build:alma9
    # Desomentar para MAC ARM
    # platform: linux/amd64
    volumes:
      - ../../consul_gva:/source:ro
      - ${SVN_PATH:-../../consul_gva_svn}:/app
